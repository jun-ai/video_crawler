# Sensitive: 真实 .env (含阿里云/DB 密钥)
.env
backend/.env

# Backups (gitignore all *.bak.* — 历史已暴露密钥事故 .env.bak.* + admin.py.bak.* 不能 commit)
*.bak.*

# Generated / large data
data/
logs/
mysql_data/
*.tar
*.tar.gz

# Python
__pycache__/
*.pyc
.venv/
.pytest_cache/

# Node
node_modules/
dist-cache/

# Whisper models
whisper_models/

# Frontend build artifact (build 产物不进 git, ECS 从 host bind-mount serve)
frontend/dist/
